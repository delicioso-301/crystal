<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/css.ejs') %>
    <title>Crystal</title>
</head>
<body>
    <header>

    </header>
    <main>
        <form action="/details" method="POST">
            <input type="text" name="day" min="1" max="31" class='dob' placeholder="DD" onchange="if(parseInt(this.value,10)<10)this.value='0'+this.value;">
            <input type="text" name="month" min="1" max="12" class='dob' placeholder="MM" onchange="if(parseInt(this.value,10)<10)this.value='0'+this.value;">
            <input type="text" name="year" min="1900" max="2020" class='dob' placeholder="YYYY">
            <select name="planets" id="planets">
                <option>SELECT PLANET</option>
                <option value="PLUTO">PLUTO</option>
                <option value="EARTH'S MOON">EARTH'S MOON</option>
                <option value="MARS">MARS</option>
                <option value="VENUS">VENUS</option>
            </select>
            <input type="submit" value="Discover">
        </form>
    </main>
    <footer>
        
    </footer>
</body>
</html>